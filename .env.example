# =========================================================
# 库存调拨规划器示例环境变量配置
# 复制此文件为 .env 后填入实际值即可生效（通过 dotenv 加载）
# =========================================================

# ---------------------------------------------------------
# 服务端口
# ---------------------------------------------------------
PORT=3000

# ---------------------------------------------------------
# Node 运行环境，影响默认日志级别
#   development → debug   开发期细节可见
#   test        → warn    测试输出保持干净
#   production  → error   仅致命错误，降低 IO 成本
# ---------------------------------------------------------
NODE_ENV=development

# ---------------------------------------------------------
# 日志级别（可选，显式覆盖 NODE_ENV 推导的默认值）
# 合法值：error | warn | info | http | verbose | debug | silly
# 未设置时按上表策略自动选择
# ---------------------------------------------------------
# LOG_LEVEL=debug

# ---------------------------------------------------------
# SQLite 数据库路径
# 留空则使用默认路径 data/inventory.db
# ---------------------------------------------------------
# DB_PATH=/var/lib/inventory-planner/inventory.db
